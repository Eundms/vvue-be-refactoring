package com.exciting.vvue.memory.repository.jpa;

import org.springframework.data.jpa.repository.JpaRepository;

import com.exciting.vvue.memory.model.PlaceMemoryImage;

public interface PlaceMemoryImageJpaRepository extends JpaRepository<PlaceMemoryImage, Long> {
}
